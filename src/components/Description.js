import React from 'react';


const Description = () => {
  return(
    <>
      <h1>Render props</h1>
      <p><i>Render props es un patrón utilizado en React que 
        consiste en delegar lo que un componente va a renderear
        a otro componente, la mayoría de la veces, a un padre en el árbol de componentes.
      </i></p>
    </>
)
}

export default Description;

